# 重点リファクタリング対象（Hotspots）

## 目的

変更頻度/複雑度/不具合リスクが高い箇所（ホットスポット）を優先度付きで整理し、段階的にリファクタリングを進めるための基準とする。

前提

- 仕様変更・機能追加は原則しない（挙動互換を維持）
- 変更は対象ごとに PR を分割し、テストとセットで進める

## 優先度定義

- Priority A: PoC導線/運用/移行に直結し、失敗コストが高い
- Priority B: 重要だが当面の影響は限定的
- Priority C: 改善余地はあるが後回しでよい

## Priority A（今期スコープ: 6件）

| 対象                                          | 痛み（現象）                                                                                                                                                                      | リスク                                                             | 目的                                                                         | 変更方針                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 受け入れ条件                                                                                   | 影響範囲               |
| --------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ | ---------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ---------------------- |
| `packages/backend/src/routes/projects.ts`     | #1914後は 195 行のcomposition root。project lifecycle/hierarchy/membership/reassignment は `src/application/projects/useCases.ts`、task/WBS/dependency/baseline は `src/application/projects/taskUseCases.ts` と `src/routes/projects/tasks.ts`、milestone/recurring template は `src/application/projects/milestoneUseCases.ts` / `recurringTemplateUseCases.ts` と専用route moduleへ抽出済み | 回帰時の影響が大きい（工数/タスク/マイルストーン等の主要機能） | #1915で projects coverage最終gateを追加済み。後続PRでは残る周辺route/service責務を必要に応じて追加する | `projects.ts` は HTTP schema/preHandler/DTO/response に寄せ、application use case は Fastify 非依存の actor/audit/ports で orchestration を担う。#1912で Org & Project→Notifications/Workflow 直接import 3件を削減し、#1913で task route/baseline orchestration、#1914で milestone/recurring template orchestrationをroute外へ移動。 | project application unit tests、project task/milestone/recurring application unit tests、project member route test、projects coverage gate、bounded-context gate、core E2E が通る | backend / e2e |
| `scripts/migrate-po.ts`                       | 超巨大（約 2800 行）。CSV/UUID 等の汎用処理が混在                                                                                                                                 | 移行リハーサル時の事故・調査コスト増                               | 変換/パースの純粋部分を抽出し、再利用/テスト可能にする                       | CSV/UUID 等の純粋関数を backend の `src/migration/` に移し、migration script はオーケストレーションに寄せる                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 抽出した関数に unit test がある。既存 runbook の手順が破綻しない                               | migration / docs       |
| `packages/backend/src/routes/integrations.ts` | #1880/#1881 service extraction 後はdefault 1500行gate内。外部連携設定・wellbeing export・statutory importが残る                                                                   | 外部連携/会計連携の回帰調査コストが高い                            | 連携種別ごとの application service へ抽出する                                | #1880で社員マスタ・勤怠確定値・会計ICS exportのDTO/CSV/dispatch/log listingを `services/integrationExports.ts` へ抽出済み。#1881 first sliceで accounting mapping rule CRUD/reapply を `services/accountingMappingRules.ts` へ抽出。#1881 second sliceで manual run / retry / scheduled run / metrics を `services/integrationRuns.ts` へ抽出。#1881 third sliceで export job list/redispatch orchestration を `services/integrationExportJobs.ts` へ抽出。#1881 fourth sliceで reconciliation summary response shaping と details CSV generation を `services/integrationReconciliation.ts` へ寄せた。#1881 fifth sliceで HR leave export payload/dispatch/log listing を `services/integrationLeaveExports.ts` へ抽出。#1881 sixth/final sliceで attendance closing create/list/summaries orchestration を `services/attendanceClosings.ts` へ抽出した。#1882でtemporary allowlistから外し、integrations subset coverage gateを追加した。次段階は別Issueで integration settings / wellbeing export / statutory import を分ける | route は認可/DTO/HTTP応答に寄せ、各 service に unit/integration test を追加                    | backend / integrations |
| `packages/backend/src/routes/auth.ts`         | #1907後は route composition の小ファイル。auth route modules はすべてdefault 1500行gate内。local login、identity admin、local credential管理は専用route/application serviceへ分離 | 認証/認可変更の事故影響が大きい                                    | 認証フローごとの service 境界と coverage gate を固定する                     | #1906でGoogle BFF/session/CSRF/logout route登録を `routes/auth/googleSessionRoutes.ts`、共通HTTP policyを `routes/auth/http.ts` へ分割済み。#1907でlocal login/password rotation、user identity admin、local credential adminを `routes/auth/*Routes.ts` と `application/auth/localIdentity*.ts` へ分離。#1908でauth temporary route allowlistなしを確認し、`coverage:auth:check` の対象漏れ/stale entry検出とbaseline閾値（statements/lines 89.7%、branches 70.5%、functions 97.9%）を固定                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 既存 auth 系 route tests、auth scope completeness test、`coverage:auth:check` が通る           | backend / security     |
| `packages/backend/src/routes/chatRooms.ts`    | #1911後は 1228 行でdefault 1500行gate内。message/AI summary/ack route は `routes/chatRooms/messages.ts`、共通access helper は `routes/chatRooms/shared.ts` へ分割済み             | チャット権限/通知の境界違反が増えやすい                            | room/message orchestration と通知/ACLを分離し、coverage gateで低下を検知する | #1909で room provisioning/lifecycle/membership service を抽出。#1910で message/mention/ack-required 通知を application port / adapter 境界へ分離し、Chat→Notifications既知違反を5件削減。#1911で `routes/chatRooms.ts` をdefault 1500行gateへ統合し、chat route/module/service/application/adapter subset の `coverage:chat:check` を既存必須 `CI / backend` jobへ追加。次段階は `routes/chat.ts` 側の ack / attachment / route module 分割を進める                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | chat coverage completeness test、`coverage:chat:check`、chat room route tests、core E2E が通る | backend / frontend e2e |
| `packages/backend/src/routes/validators.ts`   | 約2600行だった schema 集約ファイル                                                                                                                                                | route 追加時に単一ファイルへ変更が集中し、レビュー単位が大きくなる | ドメイン別 validator ファイルへ分割する                                      | `routes/validators/*.ts` へ business/chat-leave/workflow/integrations/settings-auth 単位で分割し、`validators.ts` は barrel にする                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 各 validator ファイルが 800行未満で、既存 schema tests/build/lint が通る                       | backend                |

## Priority B（候補）

| 対象                                                                | 理由                                           | 次の一手                                                                                                     |
| ------------------------------------------------------------------- | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `scripts/backup-prod.sh`                                            | 大きく多分岐。環境変数/失敗時の扱いが複雑      | dry-run/最小テストの追加 → 段階的に関数分割                                                                  |
| `packages/backend/src/services/reports.ts`                          | 集計ロジックが肥大しやすい                     | 主要集計の pure 部分を抽出し unit test                                                                       |
| `packages/frontend/src/sections/AdminSettings.tsx` / `RoomChat.tsx` | UI表示、サーバ状態、フォーム状態が混在しやすい | `docs/architecture/frontend-server-state.md` に沿って category / message list / search / hook 境界へ段階分割 |

## Bounded-context import violations

`docs/architecture/greenfield-ideal-design.md` の「1.1 バウンデッドコンテキスト（モジュール分割）」に対応する import 方向 gate を `npm run arch:bounded-context --prefix packages/backend` として導入した。

- 既存違反 baseline: `packages/backend/dependency-cruiser-known-violations.json`
- 既存違反一覧と削減方針: `docs/quality/bounded-context-imports.md`
- 新規違反: CI の `lint` job で fail

削減は Priority A の route/service 抽出と同じ単位で進める。特に `documents -> workflow`、`documents/chat/org-project -> notifications`、`identity-access -> ops/chat` を優先し、直接 import を application service / domain event / adapter へ置き換える。

## Route max-lines gate

backend lint では `max-lines` を error 1500 行（blank行を除外）で有効化する。既存の超過 route は下表の個別 cap で一時的に許容し、新規ファイルまたは未登録ファイルが 1500 行を超えた場合は CI で fail させる。800 行をレビュー上の注意ラインとし、Priority A の抽出が進むたびに cap を下げる。

## Frontend max-lines gate

frontend lint では `max-lines` を error 2500 行（blank行を除外）で有効化する。Issue #1857 では `AdminSettings.tsx` を 3710 行から 2500 行未満、`RoomChat.tsx` を 2840 行から 2500 行未満へ分割し、新規の巨大化を CI lint で検知できる状態にした。

段階削減の目安:

1. category / responsibility component 抽出で 2500 行未満を維持する。
2. server-state hook 抽出後、閾値を 2000 行へ下げる。
3. 画面単位の主要 hook/component test が揃った段階で 1500 行へ下げる。

| allowlist file                      | temporary cap | 削減方針                                                        |
| ----------------------------------- | ------------: | --------------------------------------------------------------- |
| `src/routes/chat.ts`                |          1650 | ack / attachment / route module をさらに service 化             |
| `src/routes/vendorDocs.ts`          |          1700 | vendor invoice line / allocation / reconciliation を service 化 |
| `src/routes/reportSubscriptions.ts` |          1600 | schedule/run/history 処理を service 化                          |

## 進捗ログ

- 2026-01-17: Priority A の対象を確定（Issue #576 / PR #580）
- 2026-01-17: `projects.ts` のタスク依存ロジックを分離し unit test を追加（PR #581）
- 2026-01-17: `migrate-po.ts` の CSV/ID生成ユーティリティを抽出し unit test を追加（PR #582）
- 2026-07-02: bounded-context import direction gate を導入し、既存違反 baseline と削減方針を記録（Issue #1859）
- 2026-07-02: backend `max-lines` gate と route allowlist を導入し、`routes/validators.ts` をドメイン別 validator ファイルへ分割（Issue #1856）
- 2026-07-02: frontend `max-lines` gate を導入し、`AdminSettings.tsx` / `RoomChat.tsx` を責務単位に分割。TanStack Query は段階導入判断として `docs/architecture/frontend-server-state.md` に記録（Issue #1857）
- 2026-07-11: `routes/integrations.ts` から社員マスタ・勤怠確定値・会計ICS exportのDTO/CSV/dispatch/log listingを `services/integrationExports.ts` へ抽出し、route allowlist cap を 4900 から 3800 へ縮小（Issue #1880）
- 2026-07-11: `routes/integrations.ts` から accounting mapping rule CRUD/reapply の入力正規化・Prisma操作・audit を `services/accountingMappingRules.ts` へ抽出し、service unit test を追加。route allowlist cap を 3800 から 3450 へ縮小（Issue #1881 first slice）
- 2026-07-11: `routes/integrations.ts` から IntegrationRun の manual run / retry / scheduled run / metrics 集計を `services/integrationRuns.ts` へ抽出し、service unit test と既存 route test で状態遷移・audit・retry policy を固定。route allowlist cap を 3450 から 2950 へ縮小（Issue #1881 second slice）
- 2026-07-11: `routes/integrations.ts` から export job list/redispatch orchestration を `services/integrationExportJobs.ts` へ抽出し、service unit test と既存 route test で merged pagination・idempotency・race replay・audit を固定。route allowlist cap を 2950 から 2100 へ縮小（Issue #1881 third slice）
- 2026-07-11: `routes/integrations.ts` の reconciliation summary response shaping と details CSV generation を `services/integrationReconciliation.ts` へ移し、service unit test で log response mapping と spreadsheet-safe CSV を固定。route allowlist cap を 2100 から 2050 へ縮小（Issue #1881 fourth slice）
- 2026-07-11: `routes/integrations.ts` から HR leave export payload/dispatch/log listing と leave export log response helper を `services/integrationLeaveExports.ts` へ抽出し、service unit test と既存 route test で query normalization・payload shaping・idempotency/audit・log listing を固定。route allowlist cap を 2050 から 1600 へ縮小（Issue #1881 fifth slice）
- 2026-07-11: `routes/integrations.ts` から attendance closing create/list/summaries のaudit orchestration・pagination・summary response lookupを `services/attendanceClosings.ts` へ抽出し、service unit test と既存 route test でaudit metadata・pagination・not found mappingを固定。route allowlist cap を 1600 から 1500 へ縮小（Issue #1881 sixth/final slice）
- 2026-07-12: `routes/expenses.ts` の ActionPolicy/Approval/Notification/PeriodLock/Reassignment orchestration を `src/application/expenses/useCases.ts` へ抽出し、route本体を 1035 行へ縮小。default 1500行 gate 内になったため allowlist から除外（Issue #1905）
- 2026-07-12: `routes/auth.ts` から Google BFF/session/CSRF/logout route登録を `src/routes/auth/googleSessionRoutes.ts`、共通HTTP policy helperを `src/routes/auth/http.ts` へ分割。`routes/auth.ts` は 3087 行から 2415 行へ縮小し、temporary cap を 3150 から 2500 へ下げた（Issue #1906 first slice）
- 2026-07-12: local login/password rotation、user identity admin、local credential adminを `src/routes/auth/localAuthRoutes.ts`、`userIdentityAdminRoutes.ts`、`localCredentialAdminRoutes.ts` と `src/application/auth/localIdentityUseCases.ts` / `localIdentityShared.ts` へ分離。`routes/auth.ts` は route composition のみに縮小し temporary cap から除外（Issue #1907）
- 2026-07-12: `routes/integrations.ts` をtemporary route allowlistから外し、default 1500行gateへ統合。`coverage:integrations:check` を既存必須 `CI / backend` jobへ追加し、integrations route / service subset の coverage baseline を固定（Issue #1882）
- 2026-07-13: auth route modules がすべてdefault 1500行gate内であることを確認し、auth temporary max-lines allowanceなしを継続。`coverage:auth:check` のauth scope completeness/stale entry検出を追加し、baseline閾値を statements/lines 89.7%、branches 70.5%、functions 97.9% に引上げ（Issue #1908）
- 2026-07-13: `routes/chatRooms.ts` から company/department/project/private/DM room provisioning、room list/settings patch、member追加/ACL guardを `services/chatRoomProvisioning.ts` / `chatRoomLifecycle.ts` / `chatRoomMembership.ts` へ抽出。route本体は 3066 行から 2186 行へ縮小し、temporary cap を 3150 から 2250 へ下げた（Issue #1909）

- 2026-07-13: Chat message/mention/ack-required通知連携を `application/chat/chatNotificationPort.ts` / `chatNotificationEffects.ts` と `adapters/notifications/chatNotificationAdapter.ts` へ移行し、Chat route/serviceから `services/appNotifications.ts` への直接import 5件を削減。既知bounded-context違反baselineは 53 から 48 へ減少し、`routes/chat.ts` は 1592 行、`routes/chatRooms.ts` は 2064 行へ縮小。temporary cap はそれぞれ 1650 / 2100 へ下げた（Issue #1910）

- 2026-07-13: `routes/chatRooms.ts` の message / AI summary / ack request routes を `src/routes/chatRooms/messages.ts`、共通 room access helper を `src/routes/chatRooms/shared.ts` へ分割し、route本体を 1228 行へ縮小。`chatRooms.ts` をtemporary route allowlistから外してdefault 1500行gateへ統合し、`coverage:chat:check` を既存必須 `CI / backend` jobへ追加。chat coverage scope completeness / stale entry検出とbaseline閾値（statements/lines 53.4%、branches 59.4%、functions 70.1%）を固定（Issue #1911）
- 2026-07-13: `routes/projects.ts` の project lifecycle / parent hierarchy / membership / task reassignment orchestration を `src/application/projects/useCases.ts` へ抽出し、route本体を 2043 行から 1279 行へ縮小。`projects.ts` をtemporary route allowlistから外してdefault 1500行gateへ統合し、Org & Project→Notifications/Workflow 既知違反3件を削減（Issue #1912）
- 2026-07-13: project task / dependency / baseline routeを `src/routes/projects/tasks.ts` へ分離し、DB orchestrationを `src/application/projects/taskUseCases.ts` へ抽出。`routes/projects.ts` を 537 行まで縮小（Issue #1913）
- 2026-07-13: project milestone CRUD / draft invoice sync と recurring template / generation-log listing を `src/application/projects/milestoneUseCases.ts` / `recurringTemplateUseCases.ts` および `src/routes/projects/milestones.ts` / `recurring.ts` へ分離。`routes/projects.ts` を 195 行まで縮小（Issue #1914）
- 2026-07-13: `coverage:projects:check` を既存必須 `CI / backend` jobへ追加。Org & Project context registry + `src/application/projects/**` + `src/services/dueDateRule.ts` を projects coverage scope に固定し、baseline閾値（statements/lines 66.2%、branches 59.5%、functions 77.8%）と `projects.ts` temporary allowanceなしを test で検出（Issue #1915）
