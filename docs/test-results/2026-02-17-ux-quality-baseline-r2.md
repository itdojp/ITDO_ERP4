# UX最低ライン 棚卸し（R2） - 2026-02-17

対象: PoC主要導線（ダッシュボード / 日報 + ウェルビーイング / 工数入力 / 請求）
基準: `docs/ui/ux-quality.md`

## 棚卸し結果（画面 × 観点）

| 画面 | ラベル（getByLabel） | エラー可読性 | キーボード操作 | 指摘 |
| --- | --- | --- | --- | --- |
| ダッシュボード | 入力フォームほぼ無し（閲覧中心） | 通知文言は可読 | 主操作はボタン遷移で実施可 | P2: 画面固有の追加入力無し |
| 日報 + ウェルビーイング | 日報本文/対象日/履歴絞り込みはラベル取得可 | 入力エラー・送信失敗はテキスト表示あり | 主要操作（入力→送信）はキーボードで実施可 | P2: 既存実装で最低ライン充足 |
| 工数入力 | **一覧フィルタの検索/状態に明示ラベル不足** | 保存/読込失敗はテキスト表示あり | 入力フォームはTab移動可 | **P1: 一覧フィルタをラベル化** |
| 請求 | **一覧フィルタの検索/状態に明示ラベル不足** | 金額バリデーション/API失敗はテキスト表示あり | フォーム入力はTab移動可 | **P1: 一覧フィルタをラベル化** |

## 優先度
- P0: なし
- P1:
  1. 工数一覧フィルタ（検索）のラベル付与
  2. 工数一覧フィルタ（状態）のラベル付与
  3. 請求一覧フィルタ（検索/状態）のラベル付与
- P2: ダッシュボード/日報の微調整（現時点は後回し可）

## 本PRでの対応
- `TimeEntries.tsx`:
  - フィルタ `Input` に `label="工数検索"` を追加
  - フィルタ `Select` に `label="工数状態"` を追加
- `Invoices.tsx`:
  - フィルタ `Input` に `label="請求検索"` を追加
  - フィルタ `Select` に `label="請求状態"` を追加
- 既存の `frontend-ux-quality.spec.ts`（labels/errors/keyboard）で最低ライン回帰を継続監視

## 実施確認
- `npm run typecheck --prefix packages/frontend`
- `npm run build --prefix packages/frontend`
